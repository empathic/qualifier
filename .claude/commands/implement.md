Please break this down into a list of tasks to get us to S-tier and then spin
off implementation to sub agents.  After each sub-agent implementation, spin off
a sub-agent to do verification of implementation and code review.  If iteration
is required, repeat this process until S-tier for each task.  Consider whether
these tasks may be done in parallel and do so where possible.  Don't use
worktrees.
